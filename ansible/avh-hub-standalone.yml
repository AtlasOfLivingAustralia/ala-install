- hosts: avh-hub-standalone
  roles:
    - common
    - java
    - webserver
   # - { role: nginx_vhost, hostname: "{{ biocache_hub_hostname }}", server_port: "{{ biocache_hub_server_port }}, context_path: "{{ biocache_hub_context_path }}" }
    - { role: biocache-hub-jar, biocache_hub: avh-hub, grouped_facets_json: grouped_facets_avh.json }