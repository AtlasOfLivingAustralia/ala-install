# Create a collection of species informaton systems on the AWS cassandra instance
- hosts: aws-cassandra-prod
  roles:
    - common
    - java
    - cassandra
    - biocache-properties
    - biocache-cli
    - biocache-db
    - nameindex
    - jenkins
