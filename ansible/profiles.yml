- hosts: profiles:sandbox
  roles:
    - common
    - java
    - {role: db-backup, db: mongo}
    - {role: mongodb, mongo_version: "3.4.6"}
    - cassandra
    - tomcat
    - webserver
    - postfix
    - nameindex
    - elasticsearch
    - biocache-db
    - biocache-properties
    - biocache-service
    - biocache-cli
    - sandbox
    - solr # must be installed AFTER biocache & sandbox, or you get errors complaining about not finding 'collection1'
    - profile-service
    - profile-hub
