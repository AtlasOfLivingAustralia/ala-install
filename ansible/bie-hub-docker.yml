---
- hosts: bie-hub
  vars:
    - use_docker: true
    - tomcat_user: "{{ docker_default_user }}"
    - nginx_conf_dir: "{{ data_dir }}/nginx"
    - nginx_user: "{{ docker_default_user }}"
    - skip_handlers: true
  roles:
    - bie-hub
