- include: ../../common/tasks/setfacts.yml 
  tags:
    - java

- set_fact:
    java_package: "{{ 'openjdk-11-' + java_package_suffix[java_headless | bool][java_jre | bool] }}"
  tags:
    - java

- name: install openjdk 11 java (Debian)
  apt: 
    pkg:
    - ca-certificates
    - "{{ java_package }}"
    state: latest
  when: ansible_os_family == "Debian"
  tags:
    - java
