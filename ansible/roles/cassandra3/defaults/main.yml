---

# Customise both of these for larger or smaller heap sizes
cassandra_max_heap_size: "2G"
cassandra_heap_newsize: "400M"
cassandra_rpc_address: 0.0.0.0
cassandra_cluster_name: "ALA"
cassandra_docker_dir: "{{ data_dir }}/cassandra3/docker"
cassandra_docker_data_dir: "{{ data_dir }}/cassandra3/data"
