- include: ../../common/tasks/setfacts.yml
  tags:
    - ecodata
    - deploy

- name: set ecodata_user as default value
  set_fact:
    ecodata_user: "{{ (exec_jar) | ternary ('ecodata', 'tomcat7')}}"
  tags:
    - ecodata
    - deploy

#
# External configuration directories and files
#
- name: ensure target directories exist [data subdirectories etc.]
  file: path={{item}} state=directory owner='mongodb' group='mongodb'
  with_items:
    - "{{data_dir}}/mongodb"
  tags:
    - properties
    - ecodata

- name: add jar and service
  include_role:
    name: exec-jar
  vars:
    service_name: "ecodata"
    jar_url: '{{ ecodata_artifact_url }}'
  tags:
    - deploy
    - service
    - ecodata
  when: exec_jar

- name: ensure target directories exist [data subdirectories etc.]
  file: path={{item}} state=directory owner={{ecodata_user}} group={{ecodata_user}}
  with_items:
    - "{{data_dir}}/ecodata/config"
    - "{{data_dir}}/ecodata/uploads"
    - "{{data_dir}}/ecodata/models"
    - "{{data_dir}}/ecodata/elasticsearch"
  tags:
    - properties
    - ecodata

- name: copy all config.properties
  template: src={{ecodata_properties_file | default('ecodata-config.properties')}} dest={{data_dir}}/ecodata/config/ecodata-config.properties
  tags:
    - properties
    - ecodata

- name: copy logback config
  template:
    src: logback.groovy
    dest: "{{data_dir}}/ecodata/config/logback.groovy"
    owner: "{{ecodata_user}}"
    group: "{{ecodata_user}}"
  notify:
    - restart ecodata
  tags:
    - properties
    - ecodata
  when: exec_jar

- name: ensure ecodata log directory exists
  file: path={{item}} state=directory owner={{ecodata_user}} group={{ecodata_user}}
  with_items:
    - "/var/log/atlas/ecodata"
  tags:
    - properties
    - ecodata
  when: exec_jar

- name: set ecodata log ownership
  file: path=/var/log/atlas/ecodata owner='{{ecodata_user}}' group='{{ecodata_user}}' recurse=true
  notify:
    - restart ecodata
  tags:
    - properties
    - ecodata
  when: exec_jar

#
# Copy sensitive data xml file
#
- name: Copy config files to config directory
  copy: src=sensitive-species-data.xml dest={{data_dir}}/ecodata/config/sensitive-species-data.xml
  tags:
    - ecodata
    - properties

#
# Copy default/example models files.  Don't override if already present
# as the versions in ansible are quick start for new installations.
#
- name: Copy models files to config directory
  copy: src=models/ dest={{data_dir}}/ecodata/models/ force=no
  tags:
    - ecodata
    - properties


- name: set data ownership
  file: path={{data_dir}}/ecodata owner='{{ecodata_user}}' group='{{ecodata_user}}' recurse=true
  tags:
    - properties
    - ecodata


#
# WAR file deployment and virtual host configuration
#
- include: ../../apache_vhost/tasks/main.yml context_path='{{ ecodata_context_path }}' hostname='{{ ecodata_hostname }}'
  tags:
    - deploy
    - apache_vhost
    - ecodata
  when: not webserver_nginx

- name: set nginx proxy target if configured
  set_fact:
    ecodata_proxy_target: "{{ecodata_context_path}}"
  tags:
    - nginx_vhost
    - deploy
    - ecodata
  when: webserver_nginx and ecodata_proxy_target is not defined

- name: add nginx vhost if configured
  include_role:
    name: nginx_vhost
  vars:
    appname: "ecodata"
    hostname: "{{ ecodata_hostname }}"
    context_path: "{{ ecodata_context_path }}"
    tomcat_server_port: "{{ ecodata_server_port }}"
    nginx_paths:
      - path: "{{ecodata_proxy_target}}/uploads"
        sort_label: "1_upload"
        is_proxy: false
        alias: "{{ upload_dir | default('/data/ecodata/uploads') }}"
      - path: "{{ecodata_context_path}}"
        sort_label: "2_ws"
        is_proxy: true
        proxy_pass: "http://127.0.0.1:{{ ecodata_server_port }}{{ecodata_proxy_target}}"
  tags:
    - nginx_vhost
    - deploy
    - ecodata
  when: webserver_nginx

- include: ../../tomcat_deploy/tasks/main.yml war_url='{{ ecodata_war_url }}' context_path='{{ecodata_context_path}}' hostname='{{ ecodata_hostname }}'
  tags:
    - deploy
    - tomcat_vhost
    - ecodata
  when: not exec_jar

#
# Override the default memory settings for Tomcat to increase heap space and change the garbage collector
#
- name: Override default tomcat memory configuration
  lineinfile:
    dest={{tomcat_conf}}
    regexp="^JAVA_OPTS"
    line='JAVA_OPTS="{{tomcat_java_opts_override}}"'
  notify:
    - restart tomcat
  tags:
    - ecodata
    - deploy
    - tomcat
  when: not exec_jar

#
# Cron Job configuration
#
- name: install cron job for indexing elastic search
  cron: minute=21 hour=22 name="Elastic search indexing" job="wget -O - {{ ecodata_url }}{{ ecodata_context_path }}/admin/reIndexAll >/dev/null 2>&1" user=root state=present

# if you are using mongo version 3.4, set mongodb_version4=false
- name: updating blindIp in both Primary and Secondary Server for mongodb 4.0
  replace:
    path: /etc/mongod.conf
    regexp: 'bindIp: 127.0.0.1'
    replace: 'bindIpAll: true'
  notify:
    - restart mongod
  tags: mongodb-org
  when: "{% if mongo_replica_enabled and mongo_version4 %} True {% else %} False {% endif %}"

- name: Updating blindIP for primary server for mongodb 3.4
  replace:
    path: /etc/mongod.conf
    regexp: 'bindIp: 127.0.0.1'
    replace: "bindIp: {{ groups['primary'][0] }},127.0.0.1"
  notify:
    - restart mongod
  tags: mongodb-org
  when: "{% if mongo_replication and mongo_version3 and replace_blindIp_Primary_Node %} True {% else %} False {% endif %}"

- name: Updating blindIP for secondary server for mongodb 3.4
  replace:
    path: /etc/mongod.conf
    regexp: 'bindIp: 127.0.0.1'
    replace: "bindIp: {{ groups['secondary'][0] }},127.0.0.1"
  notify:
    - restart mongod
  tags: mongodb-org
  when: "{% if mongo_version3 and replace_blindIp_Secondary_Node and copy_mongorc_to_secondary_node %} True {% else %} False {% endif %}"


- name: Insert replication set name both Server
  blockinfile:
    path: /etc/mongod.conf
    block: |
      replication:
        replSetName: rs0
  notify:
    - restart mongod
  tags: mongodb-org

  # Copy the keyfile for authentication
- name: copy security key file to nodes
  copy: src={{ecodata_local_authentication_keyFile}} dest=/etc/mongodb-keyFile mode=0400
  when:
    mongo_replica_enabled

# changes access right of the key file for mongo,
- name: change access right of key file
  file: path=/etc/mongodb-keyFile state=file owner=mongodb group=mongodb mode=0400

- name: Stop mongod db server
  service:
    name: mongod
    state: stopped

- name: Start mongod server
  service:
    name: mongod
    state: started

- name: wait connection to mongo port
  wait_for: host=0.0.0.0 port=27017 state="started" delay=5
  when: "{% if mongo_replication and mongo_version4 %} True {% else %} False {% endif %}"

# copying script to create a replica set
- name: copy initrs script
  template:
    src: initRS.j2
    dest: ./initRS.j2
  when:
    mongo_replication

## Initialize the replica set in mongo version 3.4
- name: Initialize the replica set in primary node for mongo version 3.4
  command: mongo --host "{{ groups['primary'][0] }}" ./initRS.j2
  when: "{% if mongo_replication and mongo_version3 %} True {% else %} False {% endif %}"

## Initialize the replica set in mongo version 4.x
- name: Initialize the replica set primary node for mongo version 4.x
  command: mongo ./initRS.j2
  when: "{% if mongo_replication and mongo_version4 %} True {% else %} False {% endif %}"

# Copy the mongorc to the secondary node. do connect between primary and secondary node without complication
- name: copying mongorc to secondary mongo replica set
  template:
    src: mongorc.js
    dest: /etc/mongorc.js
  when:
    copy_mongorc_to_secondary_node

- name: Ensure replicaset rs0 exists
  mongodb_replicaset:
    login_host: "{{ groups['primary'][0] }}"
    login_port: 27017
    replica_set: rs0
    members:
      - "{{ groups['primary'][0] }}:27017"
      - "{{ groups['secondary'][0] }}:27017"
    validate: no
  when:  mongo_replication

#creating admin user only in primary server. because it a replica set admin it will sync in both primary and secondary
# https://docs.ansible.com/ansible/latest/modules/mongodb_user_module.html
- name: Create admin user if necessary
  mongodb_user:
    login_host: "{{ groups['primary'][0] }}"
    database: "{{ mongodb_root_database | default('admin') }}"
    name: "{{ mongodb_root_username }}"
    password: "{{ mongodb_root_password }}"
    replica_set: "{{replicaSetName}}"
    state: present
    roles: readWriteAnyDatabase
  tags: mongodb-org
  when:
    create_user_in_primary_node

# this will added for the ecodata.
- name: Enable security
  blockinfile:
    path: /etc/mongod.conf
    backup: yes
    marker: '# {mark} Enable mongodb security authorization'
    block: |
      security:
        authorization: enabled
        keyFile: {{ecodata_keyFile}}
  notify:
    - restart mongod
  tags: mongodb-org

 #Mongo users and database
- name: Create the ecodata database and user
  mongodb_user:
    login_host: "{{ groups['primary'][0] }}"
    login_user: "{{ mongodb_root_username }}"
    login_password: "{{ mongodb_root_password }}"
    database: "{{ ecodata_database }}"
    replica_set: "{{replicaSetName}}"
    name: "{{ ecodata_username }}"
    password: "{{ ecodata_password }}"
    roles:
      - { db: "{{ ecodata_database }}", role: "dbAdmin" }
      - { db: "{{ ecodata_database }}", role: "readWrite" }
    state: present
  tags:
    - mongodb-org
  when:
    create_user_in_primary_node
