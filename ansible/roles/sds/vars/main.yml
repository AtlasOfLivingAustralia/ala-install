# application versions  
sds_config: "{{sds}}-config.properties"
# assets
version: "{{ sds_version | default('LATEST') }}"
artifactId: "sds-webapp2"
groupId: "au.org.ala"
sds_webapp_url: "{{maven_repo_ws_url}}"
