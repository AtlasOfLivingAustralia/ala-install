version: "{{ image_service_version | default('3.0.0-SNAPSHOT') }}"
artifactId: "image-service"
image_service_url: "{{maven_repo_ws_url}}"
classifier: "{{ images_classifier | default('exec') }}"
groupId: "au.org.ala"
packaging: "jar"
image_service_jar_url: "{{maven_repo_ws_url}}"
nginx_client_max_body_size: "{{ image_service_nginx_client_max_body_size | default('2048m') }}"
