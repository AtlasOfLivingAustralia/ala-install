version: "{{ image_service_version | default('2.0.0-SNAPSHOT') }}"
artifactId: "image-service"
image_service_url: "{{maven_repo_ws_url}}"
classifier: 'exec'
groupId: "au.org.ala"
packaging: "{{ (image_service_version is version('2.0.0', '>=')) | ternary('war', 'jar') }}"
image_service_jar_url: "{{maven_repo_ws_url}}"
