# assets
version: "{{ spatial_hub_version | default('LATEST') }}"
artifactId: "{{ spatial_hub_artifact_id | default('spatial-hub') }}"
classifier: "{{ spatial_hub_classifier | default('') }}"
packaging: "war"
groupId: "{{ spatial_hub_group_id | default('au.org.ala') }}"
spatial_hub_war_url: "{{ spatial_hub_maven_repo_ws_url | default(maven_repo_ws_url) }}"
default_area_name: "{{ spatial_default_area_name | default('Australia') }}"
default_area_sqkm: "{{ spatial_default_area_sqkm | default(16322156.76) }}"
default_area_bbox: "{{ spatial_default_area_bbox | default([112, -44, 154, -9]) }}"
