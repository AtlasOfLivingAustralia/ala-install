# assets
artifactId: "ala-name-matching"
classifier: 'distribution'
packaging: "zip"
ala_names_distribution_url: "{{ maven_repo_ws_url }}"
ala_names_version: "{{ ala_names_version_to_use | default('LATEST') }}"
